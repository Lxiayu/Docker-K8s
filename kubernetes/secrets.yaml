# WARNING: These are PLACEHOLDER values. You MUST replace them with
# base64-encoded secrets before deploying to any non-local environment.
# Generate secrets with: echo -n 'your-secret' | base64
apiVersion: v1
kind: Secret
metadata:
  name: database-secret
  labels:
    app: cicd-backend
type: Opaque
stringData:
  password: "CHANGE_ME_DATABASE_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  labels:
    app: cicd-backend
type: Opaque
stringData:
  secret: "CHANGE_ME_JWT_SECRET"
